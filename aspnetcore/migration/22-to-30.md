---
title: Migrar do ASP.NET Core 2,2 para a versão prévia 3,0
author: tdykstra
description: Saiba como migrar um projeto ASP.NET Core 2,2 para ASP.NET Core 3,0.
ms.author: tdykstra
ms.custom: mvc
ms.date: 07/29/2019
uid: migration/22-to-30
ms.openlocfilehash: 4e20e4248957e3db796c7bbf85baf94fa263955b
ms.sourcegitcommit: 7001657c00358b082734ba4273693b9b3ed35d2a
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/31/2019
ms.locfileid: "68670055"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="ccd3c-103">Migrar do ASP.NET Core 2,2 para 3,0</span><span class="sxs-lookup"><span data-stu-id="ccd3c-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="ccd3c-104">Por [Scott Addie](https://github.com/scottaddie) e [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="ccd3c-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="ccd3c-105">Este artigo explica como atualizar um projeto existente do ASP.NET Core 2,2 para ASP.NET Core 3,0.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="ccd3c-106">Pré-requisitos</span><span class="sxs-lookup"><span data-stu-id="ccd3c-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="ccd3c-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="ccd3c-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="ccd3c-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="ccd3c-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="ccd3c-109">Visual Studio para Mac</span><span class="sxs-lookup"><span data-stu-id="ccd3c-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="ccd3c-110">Atualizar o arquivo de projeto</span><span class="sxs-lookup"><span data-stu-id="ccd3c-110">Update the project file</span></span>

* <span data-ttu-id="ccd3c-111">Defina o [moniker do Framework de destino (TFM)](/dotnet/standard/frameworks) como `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="ccd3c-112">Remova qualquer `<PackageReference>` para o metapacote [Microsoft. AspNetCore. All](xref:fundamentals/metapackage) ou [Microsoft. AspNetCore. app](xref:fundamentals/metapackage-app) .</span><span class="sxs-lookup"><span data-stu-id="ccd3c-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="ccd3c-113">Remova qualquer `<PackageReference>` para o pacote [Microsoft. AspNetCore. Razor. Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) .</span><span class="sxs-lookup"><span data-stu-id="ccd3c-113">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package.</span></span>

* <span data-ttu-id="ccd3c-114">Atualize o `Version` atributo nos elementos `<PackageReference>` restantes para `Microsoft.AspNetCore.*` pacotes para a visualização atual (por exemplo, `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-114">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="ccd3c-115">Se não houver nenhuma versão 3,0 de um pacote, o pacote poderá ter sido preterido em 3,0.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-115">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="ccd3c-116">Muitos desses pacotes fazem parte do `Microsoft.AspNetCore.App` e não devem ser referenciados individualmente.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-116">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="ccd3c-117">Para obter uma lista preliminar de pacotes que não são mais produzidos em 3,0, consulte [parar de produzir pacotes para assemblies de estrutura compartilhada em 3,0 (ASPNET/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-117">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="ccd3c-118">A *estrutura compartilhada* é o conjunto de assemblies (arquivos *. dll* ) que estão instalados no computador e referenciados pelo `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-118">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="ccd3c-119">Saiba mais em [A estrutura compartilhada](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-119">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="ccd3c-120">Os assemblies de vários componentes notáveis foram removidos `Microsoft.AspNetCore.App` do em 3,0.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-120">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="ccd3c-121">Adicione `<PackageReference>` elementos se você estiver usando APIs de pacotes listados em [assemblies sendo removidos de Microsoft. AspNetCore. app 3,0 (ASPNET/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-121">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="ccd3c-122">Exemplos de componentes removidos incluem:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-122">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="ccd3c-123">A lista de assemblies que estão `Microsoft.AspNetCore.App` sendo distribuídos não foi finalizada e será alterada antes de 3,0 RTM.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-123">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="ccd3c-124">Considere o código a seguir:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-124">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="ccd3c-125">O `ReadAsAsync` método chamado no código anterior está incluído no `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-125">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="ccd3c-126">Instale o pacote NuGet [Microsoft. AspNet. WebApi. Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) para resolver o problema de compilação no 3,0.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-126">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="ccd3c-127">Adicionar [suporte a JSON.net](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-127">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="ccd3c-128">Os projetos assumem como padrão o [modelo de hospedagem em processo](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) no ASP.NET Core 3,0 ou posterior.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-128">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="ccd3c-129">Opcionalmente, você pode remover `<AspNetCoreHostingModel>` a propriedade no arquivo de projeto se seu valor `InProcess`for.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-129">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="ccd3c-130">Suporte do Json.NET</span><span class="sxs-lookup"><span data-stu-id="ccd3c-130">Json.NET support</span></span>

<span data-ttu-id="ccd3c-131">Como parte do trabalho para [melhorar a estrutura compartilhada ASP.NET Core](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), o [JSON.net](https://www.newtonsoft.com/json/help/html/Introduction.htm) foi removido da estrutura compartilhada ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-131">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="ccd3c-132">Para usar o Json.NET em um projeto ASP.NET Core 3,0:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-132">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="ccd3c-133">Adicione uma referência de pacote a [Microsoft. AspNetCore. Mvc. NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-133">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="ccd3c-134">Atualize `Startup.ConfigureServices` para chamar `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-134">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="ccd3c-135">`AddNewtonsoftJson`é compatível com os novos métodos de registro do serviço MVC:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-135">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="ccd3c-136">As configurações de Json.NET podem ser definidas na chamada `AddNewtonsoftJson`para:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-136">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="ccd3c-137">Registro do serviço MVC</span><span class="sxs-lookup"><span data-stu-id="ccd3c-137">MVC service registration</span></span>

<span data-ttu-id="ccd3c-138">ASP.NET Core 3,0 adiciona novas opções para o registro de cenários MVC `Startup.ConfigureServices`dentro do.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-138">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="ccd3c-139">Três novos métodos de extensão de nível superior relacionados aos cenários MVC `IServiceCollection` no estão disponíveis.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-139">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="ccd3c-140">Os modelos usam esses novos métodos em `UseMvc`vez de.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-140">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="ccd3c-141">No entanto, `AddMvc` continua se comportando como tem em versões anteriores.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-141">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="ccd3c-142">O exemplo a seguir adiciona suporte para controladores e recursos relacionados à API, mas não a exibições ou páginas.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-142">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="ccd3c-143">O modelo de API usa este código:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-143">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="ccd3c-144">O exemplo a seguir adiciona suporte para controladores, recursos relacionados à API e exibições, mas não páginas.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-144">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="ccd3c-145">O modelo de aplicativo Web (MVC) usa este código:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-145">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="ccd3c-146">O exemplo a seguir adiciona suporte para Razor Pages e suporte mínimo a controlador.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-146">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="ccd3c-147">O modelo de aplicativo Web usa este código:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-147">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="ccd3c-148">Os novos métodos também podem ser combinados.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-148">The new methods can also be combined.</span></span> <span data-ttu-id="ccd3c-149">O exemplo a seguir é equivalente a `AddMvc` chamar no ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-149">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="ccd3c-150">Atualizar o código de inicialização de roteamento</span><span class="sxs-lookup"><span data-stu-id="ccd3c-150">Update routing startup code</span></span>

<span data-ttu-id="ccd3c-151">Se um aplicativo chamar `UseMvc` ou `UseSignalR`, migre o aplicativo para o [Roteamento de ponto de extremidade](xref:fundamentals/routing) , se possível.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-151">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="ccd3c-152">Para melhorar a compatibilidade de roteamento de ponto de extremidade com versões anteriores do MVC, revertemos algumas das alterações na geração de URL introduzidas no ASP.NET Core 2,2.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-152">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="ccd3c-153">Se você enfrentou problemas ao usar o roteamento de ponto de extremidade em 2,2, espere melhorias no ASP.NET Core 3,0 com as seguintes exceções:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-153">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="ccd3c-154">Se o aplicativo implementar `IRouter` ou herdar `Route`do, talvez você queira evitar a migração no momento.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-154">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="ccd3c-155">Forneça comentários no [plano para migrar implementações baseadas em IRouter para roteamento de ponto de extremidade](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-155">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="ccd3c-156">Se o aplicativo acessar `RouteData.Routers` diretamente dentro do MVC, talvez você queira evitar a migração no momento.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-156">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="ccd3c-157">Forneça comentários em [diretrizes de migração para usar RouteData. roteadores](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-157">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="ccd3c-158">O roteamento de ponto de extremidade dá suporte à mesma sintaxe de padrão de rota `IRouter`e aos recursos de criação de padrão de rota como.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-158">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="ccd3c-159">O roteamento de `IRouteContraint`ponto de extremidade dá suporte.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-159">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="ccd3c-160">O roteamento de `[Route]`ponto `[HttpGet]`de extremidade dá suporte a, e aos outros atributos de roteamento do MVC.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-160">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="ccd3c-161">Para a maioria dos aplicativos `Startup` , o requer apenas alterações.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-161">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="ccd3c-162">Migrar inicialização. configurar</span><span class="sxs-lookup"><span data-stu-id="ccd3c-162">Migrate Startup.Configure</span></span>

<span data-ttu-id="ccd3c-163">Conselhos gerais:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-163">General advice:</span></span>

* <span data-ttu-id="ccd3c-164">Adicionar `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-164">Add `UseRouting`.</span></span> 
* <span data-ttu-id="ccd3c-165">Se o aplicativo chamar `UseStaticFiles`, coloque `UseStaticFiles` **antes** `UseRouting`de.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-165">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="ccd3c-166">Se o aplicativo usar recursos de autenticação/autorização, `AuthorizePage` como `[Authorize]`ou, coloque a chamada `UseAuthentication` para `UseAuthorization` e **depois** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-166">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="ccd3c-167">Se o aplicativo usar recursos [CORS](xref:security/cors) , `[EnableCors]`como, coloque `UseCors` avançar.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-167">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="ccd3c-168">Substituir `UseMvc` ou `UseSignalR` por .`UseEndpoints`</span><span class="sxs-lookup"><span data-stu-id="ccd3c-168">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="ccd3c-169">Veja a seguir um exemplo de `Startup.Configure` em um aplicativo típico ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-169">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="ccd3c-170">Depois de atualizar o `Startup.Configure` código anterior:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-170">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="ccd3c-171">Diretrizes de middleware de segurança</span><span class="sxs-lookup"><span data-stu-id="ccd3c-171">Security middleware guidance</span></span>

<span data-ttu-id="ccd3c-172">O suporte para autorização e CORS é unificado em relação à abordagem de [middleware](xref:fundamentals/middleware/index) .</span><span class="sxs-lookup"><span data-stu-id="ccd3c-172">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="ccd3c-173">Isso permite o uso do mesmo middleware e funcionalidade entre esses cenários.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-173">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="ccd3c-174">Um middleware de autorização atualizado é fornecido nesta versão, e o middleware CORS é aprimorado para que ele possa entender os atributos usados pelos controladores MVC.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-174">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="ccd3c-175">CORS</span><span class="sxs-lookup"><span data-stu-id="ccd3c-175">CORS</span></span>

<span data-ttu-id="ccd3c-176">Anteriormente, o CORS poderia ser difícil de configurar.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-176">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="ccd3c-177">O middleware foi fornecido para uso em alguns casos de uso, mas os filtros MVC foram destinados para serem usados **sem** o middleware em outros casos de uso.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-177">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="ccd3c-178">Com o ASP.NET Core 3,0, recomendamos que todos os aplicativos que exigem CORS usem o middleware CORS em tandem com o roteamento de ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-178">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="ccd3c-179">`UseCors`pode ser fornecido com uma política padrão, e `[EnableCors]` `[DisableCors]` os atributos podem ser usados para substituir a política padrão quando necessário.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-179">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="ccd3c-180">No exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-180">In the following example:</span></span>

* <span data-ttu-id="ccd3c-181">O CORS está habilitado para todos os pontos de extremidade `default` com a política nomeada.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-181">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="ccd3c-182">A `MyController` classe desabilita CORS com o `[DisableCors]` atributo.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-182">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="ccd3c-183">Autorização</span><span class="sxs-lookup"><span data-stu-id="ccd3c-183">Authorization</span></span>

<span data-ttu-id="ccd3c-184">Em versões anteriores do ASP.NET Core, o suporte à autorização foi fornecido `[Authorize]` por meio do atributo.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-184">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="ccd3c-185">O middleware de autorização não estava disponível.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-185">Authorization middleware wasn't available.</span></span> <span data-ttu-id="ccd3c-186">No ASP.NET Core 3,0, o middleware de autorização é necessário.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-186">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="ccd3c-187">É recomendável colocar o middleware de autorização ASP.NET Core`UseAuthorization`() imediatamente `UseAuthentication`após.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-187">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="ccd3c-188">O middleware de autorização também pode ser configurado com uma política padrão, que pode ser substituída.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-188">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="ccd3c-189">No ASP.NET Core 3,0 ou posterior, `UseAuthorization` é chamado em `Startup.Configure`, e o seguinte `HomeController` requer um usuário conectado:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-189">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="ccd3c-190">Se o aplicativo usar um `AuthorizeFilter` filtro global no MVC, é recomendável refatorar o código para fornecer uma política para o `UseAuthorization` middleware.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-190">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="ccd3c-191">No exemplo a seguir, uma política personalizada a ser aplicada a todas as solicitações `UseAuthorization` quando é chamada, e `HomeController` o permite o acesso sem o usuário entrar no aplicativo:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-191">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="ccd3c-192">A autorização por middleware funciona sem a estrutura com qualquer conhecimento específico de autorização.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-192">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="ccd3c-193">Por exemplo, as [verificações de integridade](xref:host-and-deploy/health-checks) não têm conhecimento específico de autorização, mas as verificações de integridade podem ter uma política de autorização configurável aplicada pelo middleware.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-193">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="ccd3c-194">No exemplo a seguir, `UseAuthorization` processa a autorização sem uma política padrão, mas `/healthz` o ponto de extremidade de verificação de integridade exige que `admin` o usuário esteja na função:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-194">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="ccd3c-195">A proteção é implementada para alguns cenários.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-195">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="ccd3c-196">`UseEndpoint`o middleware lançará uma exceção se uma política de autorização ou CORS for ignorada devido ao middleware ausente.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-196">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="ccd3c-197">O suporte do Analyzer para fornecer comentários adicionais sobre a configuração incorreta está em andamento.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-197">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="ccd3c-198">Migrar sinalizador</span><span class="sxs-lookup"><span data-stu-id="ccd3c-198">Migrate SignalR</span></span>

<span data-ttu-id="ccd3c-199">O mapeamento de hubs do Signalr agora ocorre `UseEndpoints`dentro do.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-199">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="ccd3c-200">Mapeie cada Hub com `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-200">Map each hub with `MapHub`.</span></span> <span data-ttu-id="ccd3c-201">Como nas versões anteriores, cada Hub é explicitamente listado.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-201">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="ccd3c-202">No exemplo a seguir, o suporte para `ChatHub` o Hub signalr é adicionado:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-202">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="ccd3c-203">Migrar controladores MVC</span><span class="sxs-lookup"><span data-stu-id="ccd3c-203">Migrate MVC controllers</span></span>

<span data-ttu-id="ccd3c-204">O mapeamento de controladores agora ocorre dentro `UseEndpoints`do.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-204">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="ccd3c-205">Adicione `MapControllers` se o aplicativo usar o roteamento de atributos.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-205">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="ccd3c-206">Como o roteamento inclui suporte para muitas estruturas no ASP.NET Core 3,0 ou posterior, adicionar controladores roteados por atributo é aceitar.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-206">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="ccd3c-207">Substitua o seguinte:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-207">Replace the following:</span></span>

* <span data-ttu-id="ccd3c-208">`MapRoute`por`MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="ccd3c-208">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="ccd3c-209">`MapAreaRoute`por`MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="ccd3c-209">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="ccd3c-210">Como o roteamento agora inclui suporte para mais do que apenas MVC, a terminologia foi alterada para tornar esses métodos claramente desconsiderando o que eles fazem.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-210">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="ccd3c-211">Rotas convencionais, como `MapControllerRoute` , / `MapAreaControllerRoute` sãoaplicadasnaordememquesãoadicionadas./ `MapDefaultControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="ccd3c-211">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="ccd3c-212">Coloque rotas mais específicas (como rotas para uma área) primeiro.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-212">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="ccd3c-213">No exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-213">In the following example:</span></span>

* <span data-ttu-id="ccd3c-214">`MapControllers`Adiciona suporte para controladores roteados por atributo.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-214">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="ccd3c-215">`MapAreaControllerRoute`Adiciona uma rota convencional para controladores em uma área.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-215">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="ccd3c-216">`MapControllerRoute`Adiciona uma rota convencional para controladores.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-216">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="ccd3c-217">Migrar Razor Pages</span><span class="sxs-lookup"><span data-stu-id="ccd3c-217">Migrate Razor Pages</span></span>

<span data-ttu-id="ccd3c-218">O mapeamento Razor Pages agora ocorre dentro `UseEndpoints`de.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-218">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="ccd3c-219">Adicione `MapRazorPages` se o aplicativo usar Razor Pages.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-219">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="ccd3c-220">Como o roteamento de ponto de extremidade inclui suporte para muitas estruturas, adicionar Razor Pages agora é opcional.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-220">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="ccd3c-221">No exemplo a seguir, `MapRazorPages` o adiciona suporte para Razor Pages:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-221">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="ccd3c-222">Usar MVC sem roteamento de ponto de extremidade</span><span class="sxs-lookup"><span data-stu-id="ccd3c-222">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="ccd3c-223">Usar o MVC `UseMvc` via `UseMvcWithDefaultRoute` ou no ASP.NET Core 3,0 requer uma aceitação explícita dentro `Startup.ConfigureServices`do.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-223">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="ccd3c-224">Isso é necessário porque o MVC deve saber se ele pode contar com a autorização e middleware CORS durante a inicialização.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-224">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="ccd3c-225">É fornecido um analisador que avisa se o aplicativo tenta usar uma configuração sem suporte.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-225">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="ccd3c-226">Se o aplicativo exigir suporte `IRouter` herdado, `EnableEndpointRouting` desabilite usando qualquer uma das seguintes `Startup.ConfigureServices`abordagens no:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-226">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="ccd3c-227">Migrar verificações de integridade</span><span class="sxs-lookup"><span data-stu-id="ccd3c-227">Migrate health checks</span></span>

<span data-ttu-id="ccd3c-228">As verificações de integridade podem ser usadas como um *roteador-Ware* com roteamento de ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-228">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="ccd3c-229">Adicione `MapHealthChecks` para usar verificações de integridade com roteamento de ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-229">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="ccd3c-230">O `MapHealthChecks` método aceita argumentos semelhantes a `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-230">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="ccd3c-231">A vantagem de usar `MapHealthChecks` o `UseHealthChecks` over é a capacidade de aplicar a autorização e ter um controle mais refinado sobre a política de correspondência.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-231">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="ccd3c-232">No exemplo a seguir, `MapHealthChecks` é chamado para um ponto de extremidade de `/healthz`verificação de integridade em:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-232">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="ccd3c-233">HostBuilder substitui WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="ccd3c-233">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="ccd3c-234">Os modelos ASP.NET Core 3,0 usam [host genérico](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-234">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="ccd3c-235">Versões anteriores usavam [host da Web](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-235">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="ccd3c-236">O código a seguir mostra a classe gerada pelo `Program` modelo ASP.NET Core 3,0:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-236">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="ccd3c-237">O código a seguir mostra a classe gerada `Program` pelo modelo ASP.NET Core 2,2:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-237">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="ccd3c-238"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder>permanece em 3,0 e é o tipo de `webBuilder` visto no exemplo de código anterior.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-238"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="ccd3c-239"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder>será preterido em uma versão futura e substituído por `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-239"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="ccd3c-240">A alteração mais significativa de `WebHostBuilder` para `HostBuilder` está na [injeção de dependência (di)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="ccd3c-240">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="ccd3c-241">Ao usar `HostBuilder`o, você só pode <xref:Microsoft.Extensions.Configuration.IConfiguration> injetar `Startup`e <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> entrar no construtor.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-241">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="ccd3c-242">As `HostBuilder` restrições de di:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-242">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="ccd3c-243">Habilite o contêiner de DI para ser criado apenas uma vez.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-243">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="ccd3c-244">Evita os problemas de tempo de vida do objeto resultante, como a resolução de várias instâncias de singletons.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-244">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="ccd3c-245">Atualizar o código do sinalizador</span><span class="sxs-lookup"><span data-stu-id="ccd3c-245">Update SignalR code</span></span>

<span data-ttu-id="ccd3c-246">`System.Text.Json`Agora é o protocolo de Hub padrão usado pelo cliente e pelo servidor.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-246">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="ccd3c-247">No `Startup.ConfigureServices`, chame `AddJsonProtocol` para definir opções de serializador.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-247">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="ccd3c-248">**Servidor**</span><span class="sxs-lookup"><span data-stu-id="ccd3c-248">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="ccd3c-249">**Cliente:**</span><span class="sxs-lookup"><span data-stu-id="ccd3c-249">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="ccd3c-250">Alternar para Newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="ccd3c-250">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="ccd3c-251">Se você estiver usando recursos do `Newtonsoft.Json` que não têm suporte `System.Text.Json`no, poderá voltar para `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-251">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="ccd3c-252">Instale o pacote NuGet [Microsoft. AspNetCore. signalr. Protocols. NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) .</span><span class="sxs-lookup"><span data-stu-id="ccd3c-252">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="ccd3c-253">No cliente, encadeamento uma `AddNewtonsoftJsonProtocol` chamada de método para a `HubConnectionBuilder` instância:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-253">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="ccd3c-254">No servidor, encadeamento uma `AddNewtonsoftJsonProtocol` chamada de método para a chamada de `AddSignalR` método `Startup.ConfigureServices`em:</span><span class="sxs-lookup"><span data-stu-id="ccd3c-254">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="ccd3c-255">Aceitar a compilação em tempo de execução</span><span class="sxs-lookup"><span data-stu-id="ccd3c-255">Opt in to runtime compilation</span></span>

<span data-ttu-id="ccd3c-256">Em 3,0, a compilação em tempo de execução é um cenário de aceitação.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-256">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="ccd3c-257">Para habilitar a compilação em tempo https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation de execução, consulte.</span><span class="sxs-lookup"><span data-stu-id="ccd3c-257">To enable runtime compilation, see https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span></span>
