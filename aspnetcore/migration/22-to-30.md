---
title: Migrar do ASP.NET Core 2.2 a 3.0 Preview
author: tdykstra
description: Saiba como migrar um projeto do ASP.NET Core 2.2 para ASP.NET Core 3.0.
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 1499239ae94919d1b094e6e55970d9920c4da2cd
ms.sourcegitcommit: 8516b586541e6ba402e57228e356639b85dfb2b9
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/11/2019
ms.locfileid: "67814862"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="43d4a-103">Migrar do ASP.NET Core 2.2 a 3.0</span><span class="sxs-lookup"><span data-stu-id="43d4a-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="43d4a-104">Por [Scott Addie](https://github.com/scottaddie) e [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="43d4a-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="43d4a-105">Este artigo explica como atualizar um projeto existente do ASP.NET Core 2.2 para ASP.NET Core 3.0.</span><span class="sxs-lookup"><span data-stu-id="43d4a-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="43d4a-106">Pré-requisitos</span><span class="sxs-lookup"><span data-stu-id="43d4a-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="43d4a-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="43d4a-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="43d4a-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="43d4a-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="43d4a-109">Visual Studio para Mac</span><span class="sxs-lookup"><span data-stu-id="43d4a-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="43d4a-110">Atualizar o arquivo de projeto</span><span class="sxs-lookup"><span data-stu-id="43d4a-110">Update the project file</span></span>

* <span data-ttu-id="43d4a-111">Defina as [Framework Moniker (TFM) de destino](/dotnet/standard/frameworks) para `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="43d4a-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="43d4a-112">Remova qualquer `<PackageReference>` para o [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) ou [Microsoft](xref:fundamentals/metapackage-app) metapacote.</span><span class="sxs-lookup"><span data-stu-id="43d4a-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="43d4a-113">Atualizar o `Version` atributo no restante `<PackageReference>` elementos para `Microsoft.AspNetCore.*` pacotes para a visualização atual (por exemplo, `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="43d4a-113">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="43d4a-114">Se não houver nenhuma versão 3.0 de um pacote, o pacote pode foi preterido no 3.0.</span><span class="sxs-lookup"><span data-stu-id="43d4a-114">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="43d4a-115">Muitos desses pacotes são parte do `Microsoft.AspNetCore.App` e não devem ser consultados individualmente.</span><span class="sxs-lookup"><span data-stu-id="43d4a-115">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="43d4a-116">Para obter uma lista preliminar de pacotes não são mais produzidas no 3.0, consulte [pare de gerar pacotes para assemblies da estrutura compartilhada 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="43d4a-116">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="43d4a-117">O *estrutura compartilhada* é o conjunto de assemblies ( *. dll* arquivos) que são instalados no computador e referenciada por `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-117">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="43d4a-118">Saiba mais em [A estrutura compartilhada](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="43d4a-118">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="43d4a-119">Os assemblies de vários componentes importantes foram removidos do `Microsoft.AspNetCore.App` 3.0.</span><span class="sxs-lookup"><span data-stu-id="43d4a-119">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="43d4a-120">Adicione `<PackageReference>` elementos, se você estiver usando as APIs dos pacotes listados em [Assemblies que estão sendo removidos do Microsoft 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="43d4a-120">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="43d4a-121">Exemplos de componentes removidos:</span><span class="sxs-lookup"><span data-stu-id="43d4a-121">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="43d4a-122">A lista de assemblies de envio no `Microsoft.AspNetCore.App` ainda não estava finalizado e será alterado antes da RTM 3.0.</span><span class="sxs-lookup"><span data-stu-id="43d4a-122">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="43d4a-123">Considere o código a seguir:</span><span class="sxs-lookup"><span data-stu-id="43d4a-123">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="43d4a-124">O `ReadAsAsync` método chamado no código anterior está incluído no `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-124">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="43d4a-125">Instalar o [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) pacote do NuGet para resolver o problema de compilação 3.0.</span><span class="sxs-lookup"><span data-stu-id="43d4a-125">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="43d4a-126">Adicione [Json.NET suporte](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="43d4a-126">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="43d4a-127">Projetos como padrão o [modelo de hospedagem em processo](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) no ASP.NET Core 3.0 ou posterior.</span><span class="sxs-lookup"><span data-stu-id="43d4a-127">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="43d4a-128">Você pode também remover os `<AspNetCoreHostingModel>` propriedade no arquivo de projeto se o valor for `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-128">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="43d4a-129">Suporte de Json.NET</span><span class="sxs-lookup"><span data-stu-id="43d4a-129">Json.NET support</span></span>

<span data-ttu-id="43d4a-130">Como parte do trabalho para [melhorar a estrutura compartilhada do ASP.NET Core](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) foi removida da estrutura compartilhada do ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="43d4a-130">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="43d4a-131">Para usar o Json.NET em um projeto do ASP.NET Core 3.0:</span><span class="sxs-lookup"><span data-stu-id="43d4a-131">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="43d4a-132">Adicione uma referência de pacote ao [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="43d4a-132">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="43d4a-133">Atualização `Startup.ConfigureServices` chamar `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-133">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="43d4a-134">`AddNewtonsoftJson` é compatível com os novos métodos de registro de serviço do MVC:</span><span class="sxs-lookup"><span data-stu-id="43d4a-134">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="43d4a-135">Json.NET podem ser definidas na chamada para `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="43d4a-135">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="43d4a-136">Registro de serviço do MVC</span><span class="sxs-lookup"><span data-stu-id="43d4a-136">MVC service registration</span></span>

<span data-ttu-id="43d4a-137">ASP.NET Core 3.0 adiciona novas opções para o registro de cenários MVC dentro de `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-137">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="43d4a-138">Três novos métodos de extensão de nível superior relacionado a cenários MVC em `IServiceCollection` estão disponíveis.</span><span class="sxs-lookup"><span data-stu-id="43d4a-138">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="43d4a-139">Os modelos usam esses novos métodos, em vez de `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-139">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="43d4a-140">No entanto, `AddMvc` continua a se comportar como em versões anteriores.</span><span class="sxs-lookup"><span data-stu-id="43d4a-140">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="43d4a-141">O exemplo a seguir adiciona suporte para controladores e os recursos relacionados à API, mas não exibições ou páginas.</span><span class="sxs-lookup"><span data-stu-id="43d4a-141">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="43d4a-142">O modelo de API usa este código:</span><span class="sxs-lookup"><span data-stu-id="43d4a-142">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="43d4a-143">O exemplo a seguir adiciona suporte para controladores, recursos relacionados à API e modos de exibição, mas não as páginas.</span><span class="sxs-lookup"><span data-stu-id="43d4a-143">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="43d4a-144">O modelo de aplicativo Web (MVC) usa este código:</span><span class="sxs-lookup"><span data-stu-id="43d4a-144">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="43d4a-145">O exemplo a seguir adiciona o suporte para as páginas Razor e suporte de controlador mínimo.</span><span class="sxs-lookup"><span data-stu-id="43d4a-145">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="43d4a-146">O modelo de aplicativo Web usa este código:</span><span class="sxs-lookup"><span data-stu-id="43d4a-146">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="43d4a-147">Os novos métodos também podem ser combinados.</span><span class="sxs-lookup"><span data-stu-id="43d4a-147">The new methods can also be combined.</span></span> <span data-ttu-id="43d4a-148">O exemplo a seguir é equivalente a chamar `AddMvc` no ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="43d4a-148">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="43d4a-149">Atualizar o código de inicialização de roteamento</span><span class="sxs-lookup"><span data-stu-id="43d4a-149">Update routing startup code</span></span>

<span data-ttu-id="43d4a-150">Se um aplicativo chamar `UseMvc` ou `UseSignalR`, migrar o aplicativo [ponto de extremidade de roteamento](xref:fundamentals/routing) se possível.</span><span class="sxs-lookup"><span data-stu-id="43d4a-150">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="43d4a-151">Para melhorar o ponto de extremidade de roteamento de compatibilidade com versões anteriores do MVC, podemos ter revertido algumas das alterações na geração de URL, introduzida no ASP.NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="43d4a-151">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="43d4a-152">Se você estiver tendo problemas ao usar o ponto de extremidade de roteamento no 2.2, espera melhorias no ASP.NET Core 3.0 com as seguintes exceções:</span><span class="sxs-lookup"><span data-stu-id="43d4a-152">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="43d4a-153">Se o aplicativo implementa `IRouter` ou herda de `Route`, talvez você queira evitar a migração no momento.</span><span class="sxs-lookup"><span data-stu-id="43d4a-153">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="43d4a-154">Fornecer comentários no [planejar a migração de implementações de IRouter com base no ponto de extremidade de roteamento](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="43d4a-154">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="43d4a-155">Se o aplicativo acessa diretamente `RouteData.Routers` dentro do MVC, talvez você queira evitar a migração no momento.</span><span class="sxs-lookup"><span data-stu-id="43d4a-155">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="43d4a-156">Fornecer comentários no [diretrizes de migração para usar RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="43d4a-156">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="43d4a-157">A mesma sintaxe de padrão de rota e o padrão de rota recursos como de criação oferece suporte a roteamento de ponto de extremidade `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-157">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="43d4a-158">Dá suporte a roteamento de ponto de extremidade `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-158">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="43d4a-159">Dá suporte a roteamento de ponto de extremidade `[Route]`, `[HttpGet]`e os outros atributos de roteamento MVC.</span><span class="sxs-lookup"><span data-stu-id="43d4a-159">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="43d4a-160">Para a maioria dos aplicativos, apenas `Startup` requer alterações.</span><span class="sxs-lookup"><span data-stu-id="43d4a-160">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="43d4a-161">Migrar Startup</span><span class="sxs-lookup"><span data-stu-id="43d4a-161">Migrate Startup.Configure</span></span>

<span data-ttu-id="43d4a-162">Conselho geral:</span><span class="sxs-lookup"><span data-stu-id="43d4a-162">General advice:</span></span>

* <span data-ttu-id="43d4a-163">Adicionar `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-163">Add `UseRouting`.</span></span> 
* <span data-ttu-id="43d4a-164">Se o aplicativo chama `UseStaticFiles`, coloque `UseStaticFiles` **antes** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-164">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="43d4a-165">Se o aplicativo usa recursos de autenticação/autorização, como `AuthorizePage` ou `[Authorize]`, coloque a chamada para `UseAuthentication` e `UseAuthorization` **depois** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-165">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="43d4a-166">Se o aplicativo usa [CORS](xref:security/cors) recursos, como `[EnableCors]`, coloque `UseCors` Avançar.</span><span class="sxs-lookup"><span data-stu-id="43d4a-166">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="43d4a-167">Substitua `UseMvc` ou `UseSignalR` com `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="43d4a-168">A seguir está um exemplo de `Startup.Configure` em um aplicativo ASP.NET Core 2.2 típico:</span><span class="sxs-lookup"><span data-stu-id="43d4a-168">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="43d4a-169">Após a atualização anterior `Startup.Configure` código:</span><span class="sxs-lookup"><span data-stu-id="43d4a-169">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="43d4a-170">Diretrizes de middleware de segurança</span><span class="sxs-lookup"><span data-stu-id="43d4a-170">Security middleware guidance</span></span>

<span data-ttu-id="43d4a-171">Suporte para CORS e a autorização é unificado em todo o [middleware](xref:fundamentals/middleware/index) abordagem.</span><span class="sxs-lookup"><span data-stu-id="43d4a-171">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="43d4a-172">Isso permite usar o mesmo middleware e a funcionalidade entre esses cenários.</span><span class="sxs-lookup"><span data-stu-id="43d4a-172">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="43d4a-173">Um middleware de autorização atualizado é fornecido nesta versão, e Middleware CORS é aprimorado para que ele possa entender os atributos usados por controladores MVC.</span><span class="sxs-lookup"><span data-stu-id="43d4a-173">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="43d4a-174">CORS</span><span class="sxs-lookup"><span data-stu-id="43d4a-174">CORS</span></span>

<span data-ttu-id="43d4a-175">Anteriormente, o CORS pode ser difícil de configurar.</span><span class="sxs-lookup"><span data-stu-id="43d4a-175">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="43d4a-176">Middleware foi fornecido para uso em alguns casos de uso, mas filtros MVC foram se destina a ser usado **sem** o middleware em outros casos de uso.</span><span class="sxs-lookup"><span data-stu-id="43d4a-176">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="43d4a-177">Com o ASP.NET Core 3.0, é recomendável que todos os aplicativos que exigem CORS usar o Middleware do CORS em tandem com o ponto de extremidade de roteamento.</span><span class="sxs-lookup"><span data-stu-id="43d4a-177">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="43d4a-178">`UseCors` pode ser fornecido com uma política padrão, e `[EnableCors]` e `[DisableCors]` atributos podem ser usados para substituir a política padrão onde for necessário.</span><span class="sxs-lookup"><span data-stu-id="43d4a-178">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="43d4a-179">No exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="43d4a-179">In the following example:</span></span>

* <span data-ttu-id="43d4a-180">O CORS está habilitado para todos os pontos de extremidade com o `default` chamado política.</span><span class="sxs-lookup"><span data-stu-id="43d4a-180">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="43d4a-181">O `MyController` desativa da classe CORS com o `[DisableCors]` atributo.</span><span class="sxs-lookup"><span data-stu-id="43d4a-181">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="43d4a-182">Autorização</span><span class="sxs-lookup"><span data-stu-id="43d4a-182">Authorization</span></span>

<span data-ttu-id="43d4a-183">Em versões anteriores do ASP.NET Core, suporte a autorização foi fornecido por meio de `[Authorize]` atributo.</span><span class="sxs-lookup"><span data-stu-id="43d4a-183">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="43d4a-184">Middleware de autorização não estava disponível.</span><span class="sxs-lookup"><span data-stu-id="43d4a-184">Authorization middleware wasn't available.</span></span> <span data-ttu-id="43d4a-185">No ASP.NET Core 3.0, o middleware de autorização é necessária.</span><span class="sxs-lookup"><span data-stu-id="43d4a-185">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="43d4a-186">É recomendável colocar o Middleware de autorização do ASP.NET Core (`UseAuthorization`) imediatamente após `UseAuthentication`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-186">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="43d4a-187">O Middleware de autorização também pode ser configurado com uma política padrão, que pode ser substituída.</span><span class="sxs-lookup"><span data-stu-id="43d4a-187">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="43d4a-188">No ASP.NET Core 3.0 ou posterior, `UseAuthorization` é chamado no `Startup.Configure`e os seguintes `HomeController` requer entrou no usuário:</span><span class="sxs-lookup"><span data-stu-id="43d4a-188">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="43d4a-189">Se o aplicativo usa um `AuthorizeFilter` como um filtro global no MVC, é recomendável refatorar o código para fornecer uma política para o `UseAuthorization` middleware.</span><span class="sxs-lookup"><span data-stu-id="43d4a-189">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="43d4a-190">No exemplo a seguir, uma política personalizada a ser aplicado a todas as solicitações quando `UseAuthorization` é chamado e o `HomeController` permite o acesso sem que o usuário entrar no aplicativo:</span><span class="sxs-lookup"><span data-stu-id="43d4a-190">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="43d4a-191">Autorização pelo middleware funciona sem a necessidade de qualquer conhecimento específico de autorização de estrutura.</span><span class="sxs-lookup"><span data-stu-id="43d4a-191">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="43d4a-192">Por exemplo, [verificações de integridade](xref:host-and-deploy/health-checks) tem nenhum conhecimento específico de autorização, mas as verificações de integridade podem ter uma política de autorização configurável aplicada pelo middleware.</span><span class="sxs-lookup"><span data-stu-id="43d4a-192">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="43d4a-193">No exemplo a seguir `UseAuthorization` processa autorização sem uma política padrão, mas o `/healthz` ponto de extremidade de verificação de integridade requer que o usuário esteja no `admin` função:</span><span class="sxs-lookup"><span data-stu-id="43d4a-193">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="43d4a-194">Proteção é implementada em alguns cenários.</span><span class="sxs-lookup"><span data-stu-id="43d4a-194">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="43d4a-195">`UseEndpoint` middleware gera uma exceção se uma autorização ou política CORS é ignorada devido à falta de middleware.</span><span class="sxs-lookup"><span data-stu-id="43d4a-195">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="43d4a-196">Suporte de analisador para fornecer comentários adicionais sobre erros de configuração está em andamento.</span><span class="sxs-lookup"><span data-stu-id="43d4a-196">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="43d4a-197">Migrar o SignalR</span><span class="sxs-lookup"><span data-stu-id="43d4a-197">Migrate SignalR</span></span>

<span data-ttu-id="43d4a-198">Mapeamento de hubs do SignalR ocorre dentro do `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-198">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="43d4a-199">Mapear cada hub com `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-199">Map each hub with `MapHub`.</span></span> <span data-ttu-id="43d4a-200">Como nas versões anteriores, cada hub explicitamente é listado.</span><span class="sxs-lookup"><span data-stu-id="43d4a-200">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="43d4a-201">No exemplo a seguir, suporte para o `ChatHub` hub SignalR é adicionado:</span><span class="sxs-lookup"><span data-stu-id="43d4a-201">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="43d4a-202">Migrar controladores do MVC</span><span class="sxs-lookup"><span data-stu-id="43d4a-202">Migrate MVC controllers</span></span>

<span data-ttu-id="43d4a-203">Mapeamento dos controladores ocorre dentro do `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-203">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="43d4a-204">Adicionar `MapControllers` se o aplicativo usa o roteamento de atributo.</span><span class="sxs-lookup"><span data-stu-id="43d4a-204">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="43d4a-205">Como roteamento inclui suporte para muitas estruturas no ASP.NET Core 3.0 ou posterior, adicionar controladores roteada de atributo é uma opção.</span><span class="sxs-lookup"><span data-stu-id="43d4a-205">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="43d4a-206">Substitua o seguinte:</span><span class="sxs-lookup"><span data-stu-id="43d4a-206">Replace the following:</span></span>

* <span data-ttu-id="43d4a-207">`MapRoute` com o `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="43d4a-207">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="43d4a-208">`MapAreaRoute` com o `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="43d4a-208">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="43d4a-209">Uma vez que o roteamento agora inclui suporte para mais do que apenas o MVC, a terminologia foi alterada para tornar esses métodos determinam claramente o que eles fazem.</span><span class="sxs-lookup"><span data-stu-id="43d4a-209">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="43d4a-210">Rotas convencionais, como `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` são aplicadas na ordem em que eles forem adicionados.</span><span class="sxs-lookup"><span data-stu-id="43d4a-210">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="43d4a-211">Coloque as rotas mais específicas (como rotas para uma área) primeiro.</span><span class="sxs-lookup"><span data-stu-id="43d4a-211">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="43d4a-212">No exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="43d4a-212">In the following example:</span></span>

* <span data-ttu-id="43d4a-213">`MapControllers` Adiciona suporte para controladores roteada de atributo.</span><span class="sxs-lookup"><span data-stu-id="43d4a-213">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="43d4a-214">`MapAreaControllerRoute` Adiciona uma rota convencional para controladores em uma área.</span><span class="sxs-lookup"><span data-stu-id="43d4a-214">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="43d4a-215">`MapControllerRoute` Adiciona uma rota convencional para controladores.</span><span class="sxs-lookup"><span data-stu-id="43d4a-215">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="43d4a-216">Migrar de páginas do Razor</span><span class="sxs-lookup"><span data-stu-id="43d4a-216">Migrate Razor Pages</span></span>

<span data-ttu-id="43d4a-217">Mapeamento de páginas do Razor agora ocorre dentro do `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-217">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="43d4a-218">Adicionar `MapRazorPages` se o aplicativo usa as páginas do Razor.</span><span class="sxs-lookup"><span data-stu-id="43d4a-218">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="43d4a-219">Como ponto de extremidade de roteamento inclui suporte para muitas estruturas, a adição de páginas do Razor agora é opt-in.</span><span class="sxs-lookup"><span data-stu-id="43d4a-219">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="43d4a-220">No exemplo a seguir, `MapRazorPages` adiciona suporte para as páginas Razor:</span><span class="sxs-lookup"><span data-stu-id="43d4a-220">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="43d4a-221">Usar o MVC sem ponto de extremidade de roteamento</span><span class="sxs-lookup"><span data-stu-id="43d4a-221">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="43d4a-222">Usando o MVC por meio `UseMvc` ou `UseMvcWithDefaultRoute` no ASP.NET Core 3.0 requer uma explícita opt-in dentro de `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-222">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="43d4a-223">Isso é necessário porque o MVC deve saber se ele pode contar com a autorização e Middleware CORS durante a inicialização.</span><span class="sxs-lookup"><span data-stu-id="43d4a-223">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="43d4a-224">Um analisador é fornecido que avisa se o aplicativo tenta usar uma configuração sem suporte.</span><span class="sxs-lookup"><span data-stu-id="43d4a-224">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="43d4a-225">Se o aplicativo exigir herdado `IRouter` dão suporte, desabilite `EnableEndpointRouting` usando qualquer uma das seguintes abordagens em `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="43d4a-225">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="43d4a-226">Migrar as verificações de integridade</span><span class="sxs-lookup"><span data-stu-id="43d4a-226">Migrate health checks</span></span>

<span data-ttu-id="43d4a-227">Verificações de integridade podem ser usadas como um *roteador ware* com o ponto de extremidade de roteamento.</span><span class="sxs-lookup"><span data-stu-id="43d4a-227">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="43d4a-228">Adicionar `MapHealthChecks` usar integridade verifica com o ponto de extremidade de roteamento.</span><span class="sxs-lookup"><span data-stu-id="43d4a-228">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="43d4a-229">O `MapHealthChecks` método aceita argumentos semelhantes ao `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-229">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="43d4a-230">A vantagem de usar `MapHealthChecks` sobre `UseHealthChecks` é a capacidade de aplicar a autorização e ter maior controle refinado sobre a política de correspondência.</span><span class="sxs-lookup"><span data-stu-id="43d4a-230">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="43d4a-231">No exemplo a seguir `MapHealthChecks` é chamado para um ponto de extremidade de verificação de integridade em `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="43d4a-231">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="43d4a-232">Substitui o HostBuilder WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="43d4a-232">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="43d4a-233">Usam os modelos do ASP.NET Core 3.0 [Host genérico](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="43d4a-233">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="43d4a-234">Versões anteriores usadas [Host Web](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="43d4a-234">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="43d4a-235">O código a seguir mostra o modelo do ASP.NET Core 3.0 gerado `Program` classe:</span><span class="sxs-lookup"><span data-stu-id="43d4a-235">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="43d4a-236">O código a seguir mostra o modelo gerado do ASP.NET Core 2.2 `Program` classe:</span><span class="sxs-lookup"><span data-stu-id="43d4a-236">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="43d4a-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> permanece no 3.0, e é o tipo do `webBuilder` visto no exemplo de código anterior.</span><span class="sxs-lookup"><span data-stu-id="43d4a-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="43d4a-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> será preterido em uma versão futura e substituído por `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="43d4a-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="43d4a-239">A alteração mais significativa do `WebHostBuilder` à `HostBuilder` está em [injeção de dependência (DI)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="43d4a-239">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="43d4a-240">Ao usar `HostBuilder`, você apenas pode injetar <xref:Microsoft.Extensions.Configuration.IConfiguration> e <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> em `Startup`do construtor.</span><span class="sxs-lookup"><span data-stu-id="43d4a-240">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="43d4a-241">O `HostBuilder` DI restrições:</span><span class="sxs-lookup"><span data-stu-id="43d4a-241">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="43d4a-242">Habilite o contêiner de injeção de dependência a ser criado apenas uma vez.</span><span class="sxs-lookup"><span data-stu-id="43d4a-242">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="43d4a-243">Evita os problemas de tempo de vida do objeto resultante como resolução de várias instâncias de singletons.</span><span class="sxs-lookup"><span data-stu-id="43d4a-243">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="43d4a-244">Atualizar o código do SignalR</span><span class="sxs-lookup"><span data-stu-id="43d4a-244">Update SignalR code</span></span>

<span data-ttu-id="43d4a-245">`System.Text.Json` é agora o padrão Hub protocolo usado pelo cliente e servidor.</span><span class="sxs-lookup"><span data-stu-id="43d4a-245">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="43d4a-246">Na `Startup.ConfigureServices`, chame `AddJsonProtocol` para definir as opções do serializador.</span><span class="sxs-lookup"><span data-stu-id="43d4a-246">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="43d4a-247">**Servidor:**</span><span class="sxs-lookup"><span data-stu-id="43d4a-247">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="43d4a-248">**Cliente:**</span><span class="sxs-lookup"><span data-stu-id="43d4a-248">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="43d4a-249">Alternar para o newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="43d4a-249">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="43d4a-250">Se você estiver usando recursos do `Newtonsoft.Json` que não têm suporte no `System.Text.Json`, você pode alternar novamente para `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="43d4a-250">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="43d4a-251">Instalar o [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) pacote do NuGet.</span><span class="sxs-lookup"><span data-stu-id="43d4a-251">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="43d4a-252">No cliente, encadear uma `AddNewtonsoftJsonProtocol` chamada de método para o `HubConnectionBuilder` instância:</span><span class="sxs-lookup"><span data-stu-id="43d4a-252">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="43d4a-253">No servidor, encadear uma `AddNewtonsoftJsonProtocol` chamada de método para o `AddSignalR` chamada de método na `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="43d4a-253">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="43d4a-254">Aceitar a compilação de tempo de execução</span><span class="sxs-lookup"><span data-stu-id="43d4a-254">Opt in to runtime compilation</span></span>

<span data-ttu-id="43d4a-255">No 3.0, a compilação de tempo de execução é um cenário de aceitação.</span><span class="sxs-lookup"><span data-stu-id="43d4a-255">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="43d4a-256">Para habilitar a compilação de tempo de execução, consulte <xref:mvc/views/view-compilation#runtime-compilation>.</span><span class="sxs-lookup"><span data-stu-id="43d4a-256">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
