---
title: Migrar do ASP.NET Core 2.2 a 3.0 Preview
author: tdykstra
description: Saiba como migrar um projeto do ASP.NET Core 2.2 para ASP.NET Core 3.0.
ms.author: tdykstra
ms.custom: mvc
ms.date: 05/09/2019
uid: migration/22-to-30
ms.openlocfilehash: 7c47d042749ac3960c301ad918858e0375f4b2d0
ms.sourcegitcommit: 20ca93d72cb0b3998637724b44523e57b089c8fd
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/09/2019
ms.locfileid: "65473582"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="491ec-103">Migrar do ASP.NET Core 2.2 a 3.0</span><span class="sxs-lookup"><span data-stu-id="491ec-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="491ec-104">Por [Scott Addie](https://github.com/scottaddie) e [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="491ec-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="491ec-105">Este artigo explica como atualizar um projeto existente do ASP.NET Core 2.2 para ASP.NET Core 3.0.</span><span class="sxs-lookup"><span data-stu-id="491ec-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

[!INCLUDE[](~/includes/net-core-prereqs-all-3.0.md)]

## <a name="update-the-project-file"></a><span data-ttu-id="491ec-106">Atualizar o arquivo de projeto</span><span class="sxs-lookup"><span data-stu-id="491ec-106">Update the project file</span></span>

* <span data-ttu-id="491ec-107">Defina as [Framework Moniker (TFM) de destino](/dotnet/standard/frameworks#referring-to-frameworks) para `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="491ec-107">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="491ec-108">Remova qualquer `<PackageReference>` para o [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) ou [Microsoft](xref:fundamentals/metapackage-app) metapacote.</span><span class="sxs-lookup"><span data-stu-id="491ec-108">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="491ec-109">Atualizar o `Version` em demais `<PackageReference>` elementos para `Microsoft.AspNetCore.*` pacotes para a visualização atual (por exemplo, `3.0.0-preview-18579-0053`).</span><span class="sxs-lookup"><span data-stu-id="491ec-109">Update the `Version` on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview-18579-0053`).</span></span>

  <span data-ttu-id="491ec-110">Se não houver nenhuma versão 3.0 de um pacote, o pacote pode foi preterido no 3.0.</span><span class="sxs-lookup"><span data-stu-id="491ec-110">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="491ec-111">Muitos deles são parte do `Microsoft.AspNetCore.App` e não devem ser consultados individualmente.</span><span class="sxs-lookup"><span data-stu-id="491ec-111">Many of these are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="491ec-112">Para obter uma lista preliminar de pacotes não são mais produzidas no 3.0, consulte [pare de gerar pacotes para assemblies da estrutura compartilhada 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="491ec-112">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="491ec-113">O *estrutura compartilhada* é o conjunto de assemblies (*. dll* arquivos) que são instalados no computador e referenciada por `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="491ec-113">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="491ec-114">Para obter mais informações, consulte [estrutura compartilhada](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span><span class="sxs-lookup"><span data-stu-id="491ec-114">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="491ec-115">Alguns assemblies foram removidos do `Microsoft.AspNetCore.App` entre 2. x e o 3.0.</span><span class="sxs-lookup"><span data-stu-id="491ec-115">Some assemblies were removed from `Microsoft.AspNetCore.App` between 2.x and 3.0.</span></span> <span data-ttu-id="491ec-116">Talvez você precise adicionar `<PackageReference>` itens se você estiver usando as APIs dos pacotes listados em [Assemblies que estão sendo removidos do Microsoft 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="491ec-116">You may need to add `<PackageReference>` items if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="491ec-117">Por exemplo, `Microsoft.EntityFrameworkCore` e `System.Data.SqlClient` fazem parte não dos `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="491ec-117">For example, `Microsoft.EntityFrameworkCore` and `System.Data.SqlClient` are no longer part of `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="491ec-118">A lista de assemblies de envio no `Microsoft.AspNetCore.App` ainda não foram finalizados e será alterado antes da RTM 3.0.</span><span class="sxs-lookup"><span data-stu-id="491ec-118">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized yet and will change before 3.0 RTM.</span></span>

* <span data-ttu-id="491ec-119">Adicione [Json.NET suporte](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="491ec-119">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="491ec-120">Projetos como padrão o [modelo de hospedagem em processo](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) no ASP.NET Core 3.0 ou posterior.</span><span class="sxs-lookup"><span data-stu-id="491ec-120">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="491ec-121">Você pode também remover os `<AspNetCoreHostingModel>` propriedade no arquivo de projeto se o valor for `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="491ec-121">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="491ec-122">Suporte de Json.NET</span><span class="sxs-lookup"><span data-stu-id="491ec-122">Json.NET support</span></span>

<span data-ttu-id="491ec-123">Como parte do trabalho para [melhorar a estrutura compartilhada do ASP.NET Core](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) foi removida da estrutura compartilhada do ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="491ec-123">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="491ec-124">Para usar o Json.NET em um projeto do ASP.NET Core 3.0:</span><span class="sxs-lookup"><span data-stu-id="491ec-124">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="491ec-125">Adicione uma referência de pacote ao [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="491ec-125">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="491ec-126">Atualização `Startup.ConfigureServices` chamar `AddNewtonsoftJson()`.</span><span class="sxs-lookup"><span data-stu-id="491ec-126">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson()`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="491ec-127">Newtonsoft podem ser definidas com `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="491ec-127">Newtonsoft settings can be set with `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="491ec-128">Registro de serviço do MVC</span><span class="sxs-lookup"><span data-stu-id="491ec-128">MVC service registration</span></span>

<span data-ttu-id="491ec-129">ASP.NET Core 3.0 adiciona novas opções para o registro de cenários MVC dentro de `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="491ec-129">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="491ec-130">Três novos métodos de extensão de nível superior relacionado a cenários MVC em `IServiceCollection` estão disponíveis.</span><span class="sxs-lookup"><span data-stu-id="491ec-130">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="491ec-131">Os modelos usam esses novos métodos, em vez de `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="491ec-131">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="491ec-132">No entanto, `AddMvc` continua a se comportar como em versões anteriores.</span><span class="sxs-lookup"><span data-stu-id="491ec-132">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="491ec-133">O exemplo a seguir adiciona suporte para controladores e os recursos relacionados à API, mas não exibições ou páginas.</span><span class="sxs-lookup"><span data-stu-id="491ec-133">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="491ec-134">O modelo de API usa este código:</span><span class="sxs-lookup"><span data-stu-id="491ec-134">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="491ec-135">O exemplo a seguir adiciona suporte para controladores, recursos relacionados à API e modos de exibição, mas não as páginas.</span><span class="sxs-lookup"><span data-stu-id="491ec-135">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="491ec-136">O modelo de aplicativo Web (MVC) usa este código:</span><span class="sxs-lookup"><span data-stu-id="491ec-136">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="491ec-137">O exemplo a seguir adiciona o suporte para as páginas Razor e suporte de controlador mínimo.</span><span class="sxs-lookup"><span data-stu-id="491ec-137">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="491ec-138">O modelo de aplicativo Web usa este código:</span><span class="sxs-lookup"><span data-stu-id="491ec-138">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="491ec-139">Os novos métodos também podem ser combinados.</span><span class="sxs-lookup"><span data-stu-id="491ec-139">The new methods can also be combined.</span></span> <span data-ttu-id="491ec-140">O exemplo a seguir é equivalente a chamar `AddMvc` no ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="491ec-140">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="491ec-141">Atualizar o código de inicialização de roteamento</span><span class="sxs-lookup"><span data-stu-id="491ec-141">Update routing startup code</span></span>

<span data-ttu-id="491ec-142">Se um aplicativo chamar `UseMvc` ou `UseSignalR`, migrar o aplicativo [ponto de extremidade de roteamento](xref:fundamentals/routing) se possível.</span><span class="sxs-lookup"><span data-stu-id="491ec-142">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="491ec-143">Para melhorar o ponto de extremidade de roteamento de compatibilidade com versões anteriores do MVC, podemos ter revertido algumas das alterações na geração de URL, introduzida no ASP.NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="491ec-143">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="491ec-144">Se você estiver tendo problemas ao usar o ponto de extremidade de roteamento no 2.2, espera melhorias no ASP.NET Core 3.0 com as seguintes exceções:</span><span class="sxs-lookup"><span data-stu-id="491ec-144">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="491ec-145">Se o aplicativo implementa `IRouter` ou herda de `Route`, talvez você queira evitar a migração no momento.</span><span class="sxs-lookup"><span data-stu-id="491ec-145">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="491ec-146">Fornecer comentários no [planejar a migração de implementações de IRouter com base no ponto de extremidade de roteamento](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="491ec-146">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="491ec-147">Se o aplicativo acessa diretamente `RouteData.Routers` dentro do MVC, talvez você queira evitar a migração no momento.</span><span class="sxs-lookup"><span data-stu-id="491ec-147">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="491ec-148">Fornecer comentários no [diretrizes de migração para usar RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="491ec-148">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="491ec-149">A mesma sintaxe de padrão de rota e o padrão de rota recursos como de criação oferece suporte a roteamento de ponto de extremidade `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="491ec-149">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="491ec-150">Dá suporte a roteamento de ponto de extremidade `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="491ec-150">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="491ec-151">Dá suporte a roteamento de ponto de extremidade `[Route]`, `[HttpGet]`e os outros atributos de roteamento MVC.</span><span class="sxs-lookup"><span data-stu-id="491ec-151">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="491ec-152">Para a maioria dos aplicativos, apenas `Startup` requer alterações.</span><span class="sxs-lookup"><span data-stu-id="491ec-152">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="491ec-153">Migrar Startup</span><span class="sxs-lookup"><span data-stu-id="491ec-153">Migrate Startup.Configure</span></span>

<span data-ttu-id="491ec-154">Conselho geral:</span><span class="sxs-lookup"><span data-stu-id="491ec-154">General advice:</span></span>

* <span data-ttu-id="491ec-155">Adicionar `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="491ec-155">Add `UseRouting`.</span></span> 
* <span data-ttu-id="491ec-156">Se o aplicativo chama `UseStaticFiles`, coloque `UseStaticFiles` **antes** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="491ec-156">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="491ec-157">Se o aplicativo usa recursos de autenticação/autorização, como `AuthorizePage` ou `[Authorize]`, coloque a chamada para `UseAuthentication` e `UseAuthorization` **depois** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="491ec-157">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="491ec-158">Se o aplicativo usa [CORS](xref:security/cors) recursos, como `[EnableCors]`, coloque `UseCors` Avançar.</span><span class="sxs-lookup"><span data-stu-id="491ec-158">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="491ec-159">Substitua `UseMvc` ou `UseSignalR` com `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="491ec-159">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="491ec-160">A seguir está um exemplo de `Startup.Configure` em um aplicativo ASP.NET Core 2.2 típico:</span><span class="sxs-lookup"><span data-stu-id="491ec-160">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="491ec-161">Após a atualização anterior `Startup.Configure` código:</span><span class="sxs-lookup"><span data-stu-id="491ec-161">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="491ec-162">Diretrizes de middleware de segurança</span><span class="sxs-lookup"><span data-stu-id="491ec-162">Security middleware guidance</span></span>

<span data-ttu-id="491ec-163">Suporte para CORS e a autorização é unificado em todo o [middleware](xref:fundamentals/middleware/index) abordagem.</span><span class="sxs-lookup"><span data-stu-id="491ec-163">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="491ec-164">Isso permite usar o mesmo middleware e a funcionalidade entre esses cenários.</span><span class="sxs-lookup"><span data-stu-id="491ec-164">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="491ec-165">Um middleware de autorização atualizado é fornecido nesta versão, e middleware CORS é aprimorado para que ele possa entender os atributos usados por controladores MVC.</span><span class="sxs-lookup"><span data-stu-id="491ec-165">An updated authorization middleware is provided in this release, and CORS middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="491ec-166">CORS</span><span class="sxs-lookup"><span data-stu-id="491ec-166">CORS</span></span>

<span data-ttu-id="491ec-167">Anteriormente, o CORS pode ser difícil de configurar.</span><span class="sxs-lookup"><span data-stu-id="491ec-167">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="491ec-168">Middleware foi fornecido para uso em alguns casos de uso, mas filtros MVC foram se destina a ser usado **sem** o middleware em outros casos de uso.</span><span class="sxs-lookup"><span data-stu-id="491ec-168">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="491ec-169">Com o ASP.NET Core 3.0, é recomendável que todos os aplicativos que exigem CORS usar o middleware do CORS em tandem com o ponto de extremidade de roteamento.</span><span class="sxs-lookup"><span data-stu-id="491ec-169">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="491ec-170">`UseCors` pode ser fornecido com uma política padrão, e `[EnableCors]` e `[DisableCors]` atributos podem ser usados para substituir a política padrão onde for necessário.</span><span class="sxs-lookup"><span data-stu-id="491ec-170">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="491ec-171">No exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="491ec-171">In the following example:</span></span>

* <span data-ttu-id="491ec-172">O CORS está habilitado para todos os pontos de extremidade com o `default` chamado política.</span><span class="sxs-lookup"><span data-stu-id="491ec-172">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="491ec-173">O `MyController` desativa da classe CORS com o `[DisableCors]` atributo.</span><span class="sxs-lookup"><span data-stu-id="491ec-173">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="491ec-174">Autorização</span><span class="sxs-lookup"><span data-stu-id="491ec-174">Authorization</span></span>

<span data-ttu-id="491ec-175">Em versões anteriores do ASP.NET Core, suporte a autorização foi fornecido por meio de `[Authorize]` atributo.</span><span class="sxs-lookup"><span data-stu-id="491ec-175">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="491ec-176">Middleware de autorização não estava disponível.</span><span class="sxs-lookup"><span data-stu-id="491ec-176">Authorization middleware wasn't available.</span></span> <span data-ttu-id="491ec-177">No ASP.NET Core 3.0, o middleware de autorização é necessária.</span><span class="sxs-lookup"><span data-stu-id="491ec-177">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="491ec-178">É recomendável colocar o middleware de autorização (`UseAuthentication`) imediatamente antes do `UseAuthorization`.</span><span class="sxs-lookup"><span data-stu-id="491ec-178">We recommend placing the authorization middleware (`UseAuthentication`) immediately before `UseAuthorization`.</span></span> <span data-ttu-id="491ec-179">Middleware de autorização também pode ser configurado com uma política padrão, que pode ser substituída.</span><span class="sxs-lookup"><span data-stu-id="491ec-179">Authorization middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="491ec-180">No ASP.NET Core 3.0 ou posterior, `UseAuthorization` é chamado no `Startup.Configure`e os seguintes `HomeController` requer entrou no usuário:</span><span class="sxs-lookup"><span data-stu-id="491ec-180">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="491ec-181">Se o aplicativo usa um `AuthorizeFilter` como um filtro global no MVC, é recomendável refatorar o código para fornecer uma política para o `UseAuthorization` middleware.</span><span class="sxs-lookup"><span data-stu-id="491ec-181">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="491ec-182">No exemplo a seguir, uma política personalizada a ser aplicado a todas as solicitações quando `UseAuthorization` é chamado e o `HomeController` permite o acesso sem que o usuário entrar no aplicativo:</span><span class="sxs-lookup"><span data-stu-id="491ec-182">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="491ec-183">Autorização pelo middleware funciona sem a necessidade de qualquer conhecimento específico de autorização de estrutura.</span><span class="sxs-lookup"><span data-stu-id="491ec-183">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="491ec-184">Por exemplo, [verificações de integridade](xref:host-and-deploy/health-checks) tem nenhum conhecimento específico de autorização, mas as verificações de integridade podem ter uma política de autorização configurável aplicada pelo middleware.</span><span class="sxs-lookup"><span data-stu-id="491ec-184">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="491ec-185">No exemplo a seguir `UseAuthorization` processa autorização sem uma política padrão, mas o `/healthz` ponto de extremidade de verificação de integridade requer que o usuário esteja no `admin` função:</span><span class="sxs-lookup"><span data-stu-id="491ec-185">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="491ec-186">Proteção é implementada em alguns cenários.</span><span class="sxs-lookup"><span data-stu-id="491ec-186">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="491ec-187">`UseEndpoint` middleware gera uma exceção se uma autorização ou política CORS é ignorada devido à falta de middleware.</span><span class="sxs-lookup"><span data-stu-id="491ec-187">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="491ec-188">Suporte de analisador para fornecer comentários adicionais sobre erros de configuração está em andamento.</span><span class="sxs-lookup"><span data-stu-id="491ec-188">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="491ec-189">Migrar o SignalR</span><span class="sxs-lookup"><span data-stu-id="491ec-189">Migrate SignalR</span></span>

<span data-ttu-id="491ec-190">Mapeamento de hubs do SignalR ocorre dentro do `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="491ec-190">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="491ec-191">Mapear cada hub com `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="491ec-191">Map each hub with `MapHub`.</span></span> <span data-ttu-id="491ec-192">Como nas versões anteriores, cada hub explicitamente é listado.</span><span class="sxs-lookup"><span data-stu-id="491ec-192">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="491ec-193">No exemplo a seguir, suporte para o `ChatHub` hub SignalR é adicionado:</span><span class="sxs-lookup"><span data-stu-id="491ec-193">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="491ec-194">Migrar controladores do MVC</span><span class="sxs-lookup"><span data-stu-id="491ec-194">Migrate MVC controllers</span></span>

<span data-ttu-id="491ec-195">Mapeamento dos controladores ocorre dentro do `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="491ec-195">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="491ec-196">Adicionar `MapControllers` se o aplicativo usa o roteamento de atributo.</span><span class="sxs-lookup"><span data-stu-id="491ec-196">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="491ec-197">Como roteamento inclui suporte para muitas estruturas no ASP.NET Core 3.0 ou posterior, adicionar controladores roteada de atributo é uma opção.</span><span class="sxs-lookup"><span data-stu-id="491ec-197">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="491ec-198">Substitua o seguinte:</span><span class="sxs-lookup"><span data-stu-id="491ec-198">Replace the following:</span></span>

* <span data-ttu-id="491ec-199">`MapRoute` com o `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="491ec-199">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="491ec-200">`MapAreaRoute` com o `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="491ec-200">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="491ec-201">Uma vez que o roteamento agora inclui suporte para mais do que apenas o MVC, a terminologia foi alterada para tornar esses métodos determinam claramente o que eles fazem.</span><span class="sxs-lookup"><span data-stu-id="491ec-201">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="491ec-202">Rotas convencionais, como `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` são aplicadas na ordem em que eles forem adicionados.</span><span class="sxs-lookup"><span data-stu-id="491ec-202">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="491ec-203">Coloque as rotas mais específicas (como rotas para uma área) primeiro.</span><span class="sxs-lookup"><span data-stu-id="491ec-203">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="491ec-204">No exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="491ec-204">In the following example:</span></span>

* <span data-ttu-id="491ec-205">`MapControllers` Adiciona suporte para controladores roteada de atributo.</span><span class="sxs-lookup"><span data-stu-id="491ec-205">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="491ec-206">`MapAreaControllerRoute` Adiciona uma rota convencional para controladores em uma área.</span><span class="sxs-lookup"><span data-stu-id="491ec-206">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="491ec-207">`MapControllerRoute` Adiciona uma rota convencional para controladores.</span><span class="sxs-lookup"><span data-stu-id="491ec-207">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="491ec-208">Migrar de páginas do Razor</span><span class="sxs-lookup"><span data-stu-id="491ec-208">Migrate Razor Pages</span></span>

<span data-ttu-id="491ec-209">Mapeamento de páginas do Razor agora ocorre dentro do `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="491ec-209">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="491ec-210">Adicionar `MapRazorPages` se o aplicativo usa as páginas do Razor.</span><span class="sxs-lookup"><span data-stu-id="491ec-210">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="491ec-211">Como ponto de extremidade de roteamento inclui suporte para muitas estruturas, a adição de páginas do Razor agora é opt-in.</span><span class="sxs-lookup"><span data-stu-id="491ec-211">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="491ec-212">No exemplo a seguir, `MapRazorPages` adiciona suporte para as páginas Razor:</span><span class="sxs-lookup"><span data-stu-id="491ec-212">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="491ec-213">Usar o MVC sem ponto de extremidade de roteamento</span><span class="sxs-lookup"><span data-stu-id="491ec-213">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="491ec-214">Usando o MVC por meio `UseMvc` ou `UseMvcWithDefaultRoute` no ASP.NET Core 3.0 requer uma explícita opt-in dentro de `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="491ec-214">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="491ec-215">Isso é necessário porque o MVC deve saber se ele pode contar com a autorização e middleware CORS durante a inicialização.</span><span class="sxs-lookup"><span data-stu-id="491ec-215">This is required because MVC must know whether it can rely on the authorization and CORS middleware during initialization.</span></span> <span data-ttu-id="491ec-216">Um analisador é fornecido que avisa se o aplicativo tenta usar uma configuração sem suporte.</span><span class="sxs-lookup"><span data-stu-id="491ec-216">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="491ec-217">Se o aplicativo exigir herdado `IRouter` dão suporte, desabilite `EnableEndpointRouting` usando qualquer uma das seguintes abordagens em `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="491ec-217">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="491ec-218">Migrar as verificações de integridade</span><span class="sxs-lookup"><span data-stu-id="491ec-218">Migrate health checks</span></span>

<span data-ttu-id="491ec-219">Verificações de integridade podem ser usadas como um *roteador ware* com o ponto de extremidade de roteamento.</span><span class="sxs-lookup"><span data-stu-id="491ec-219">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="491ec-220">Adicionar `MapHealthChecks` usar integridade verifica com o ponto de extremidade de roteamento.</span><span class="sxs-lookup"><span data-stu-id="491ec-220">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="491ec-221">O `MapHealthChecks` método aceita argumentos semelhantes ao `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="491ec-221">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="491ec-222">A vantagem de usar `MapHealthChecks` sobre `UseHealthChecks` é a capacidade de aplicar a autorização e ter maior controle refinado sobre a política de correspondência.</span><span class="sxs-lookup"><span data-stu-id="491ec-222">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="491ec-223">No exemplo a seguir `MapHealthChecks` é chamado para um ponto de extremidade de verificação de integridade em `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="491ec-223">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="491ec-224">Substitui o HostBuilder WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="491ec-224">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="491ec-225">Usam os modelos do ASP.NET Core 3.0 [Host genérico](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="491ec-225">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="491ec-226">Versões anteriores usadas [Host Web](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="491ec-226">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="491ec-227">O código a seguir mostra o modelo do ASP.NET Core 3.0 gerado `Program` classe:</span><span class="sxs-lookup"><span data-stu-id="491ec-227">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="491ec-228">O código a seguir mostra o modelo gerado do ASP.NET Core 2.2 `Program` classe:</span><span class="sxs-lookup"><span data-stu-id="491ec-228">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="491ec-229"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> permanece no 3.0, e é o tipo do `webBuilder` visto no exemplo de código anterior.</span><span class="sxs-lookup"><span data-stu-id="491ec-229"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="491ec-230"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> será preterido em uma versão futura e substituído por `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="491ec-230"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="491ec-231">A alteração mais significativa do `WebHostBuilder` à `HostBuilder` está em [injeção de dependência (DI)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="491ec-231">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="491ec-232">Ao usar `HostBuilder`, você apenas pode injetar <xref:Microsoft.Extensions.Configuration.IConfiguration> e <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> em `Startup`do construtor.</span><span class="sxs-lookup"><span data-stu-id="491ec-232">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="491ec-233">O `HostBuilder` DI restrições:</span><span class="sxs-lookup"><span data-stu-id="491ec-233">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="491ec-234">Habilite o contêiner de injeção de dependência a ser criado apenas uma vez.</span><span class="sxs-lookup"><span data-stu-id="491ec-234">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="491ec-235">Evita os problemas de tempo de vida do objeto resultante como resolução de várias instâncias de singletons.</span><span class="sxs-lookup"><span data-stu-id="491ec-235">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="491ec-236">Atualizar o código do SignalR</span><span class="sxs-lookup"><span data-stu-id="491ec-236">Update SignalR code</span></span>

<span data-ttu-id="491ec-237">`System.Text.Json` é agora o padrão Hub protocolo usado pelo cliente e servidor.</span><span class="sxs-lookup"><span data-stu-id="491ec-237">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="491ec-238">Na `Startup.ConfigureServices`, chame `AddJsonProtocol` para definir as opções do serializador.</span><span class="sxs-lookup"><span data-stu-id="491ec-238">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="491ec-239">**Servidor:**</span><span class="sxs-lookup"><span data-stu-id="491ec-239">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="491ec-240">**Cliente:**</span><span class="sxs-lookup"><span data-stu-id="491ec-240">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="491ec-241">Alternar para o newtonsoft. JSON</span><span class="sxs-lookup"><span data-stu-id="491ec-241">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="491ec-242">Se você estiver usando recursos do `Newtonsoft.Json` que não têm suporte no `System.Text.Json`, você pode alternar novamente para `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="491ec-242">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="491ec-243">Instalar o [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) pacote do NuGet.</span><span class="sxs-lookup"><span data-stu-id="491ec-243">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="491ec-244">No cliente, encadear uma `AddNewtonsoftJsonProtocol` chamada de método para o `HubConnectionBuilder` instância:</span><span class="sxs-lookup"><span data-stu-id="491ec-244">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="491ec-245">No servidor, encadear uma `AddNewtonsoftJsonProtocol` chamada de método para o `AddSignalR` chamada de método na `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="491ec-245">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="491ec-246">Aceitar a compilação de tempo de execução</span><span class="sxs-lookup"><span data-stu-id="491ec-246">Opt in to runtime compilation</span></span>

<span data-ttu-id="491ec-247">No 3.0, a compilação de tempo de execução é um cenário de aceitação.</span><span class="sxs-lookup"><span data-stu-id="491ec-247">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="491ec-248">Para habilitar a compilação de tempo de execução, consulte <xref:mvc/views/view-compilation#runtime-compilation>.</span><span class="sxs-lookup"><span data-stu-id="491ec-248">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
